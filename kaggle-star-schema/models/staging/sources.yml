version: 2

sources:
  - name: raw
    description: Raw tables loaded from the Kaggle Excel source into DuckDB.

    schema: main

    tables:
      - name: orders
        identifier: raw_orders
        description: Raw transactional order data loaded from the source Excel workbook.

      - name: calendar
        identifier: raw_calendar
        description: Raw calendar data loaded from the source Excel workbook.
